---
phase: 01-product-shell-and-writer-profile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - tsconfig.json
  - src/app/layout.tsx
  - src/app/globals.css
  - src/app/profile/page.tsx
  - src/app/api/profile/route.ts
  - src/lib/writer-profile-schema.ts
  - src/lib/writer-profile-store.ts
  - tests/writer-profile-store.test.ts
autonomous: true
requirements:
  - PERS-01
user_setup: []
must_haves:
  truths:
    - The app boots as a Next.js App Router project and exposes a dedicated writer profile page.
    - A single-user writer profile can be created, updated, and read back through the app-owned persistence layer.
    - The stored profile shape contains the fields later phases need for tone defaults and style guardrails.
  artifacts:
    - package.json configures a runnable web app with lint, test, and build scripts.
    - src/lib/writer-profile-schema.ts defines the writer profile contract.
    - src/lib/writer-profile-store.ts and src/app/api/profile/route.ts implement real persistence, not placeholders.
    - src/app/profile/page.tsx renders a profile editing flow connected to stored data.
  key_links:
    - The profile page submits to the profile route and reloads persisted values.
    - The writer profile schema is imported by both the UI layer and the persistence layer.
---

<objective>
Stand up the product shell foundation and implement persistent single-user writer profile management.

Purpose: Later phases need a stable app shell and a reusable profile source of truth before source parsing or writing generation can build on top.
Output: A working Next.js app scaffold, a writer profile schema and store, a profile API route, and a profile management page.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-product-shell-and-writer-profile/01-CONTEXT.md
@docs/plans/2026-03-30-source-aware-writing-agent-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the web app foundation</name>
  <files>package.json, next.config.ts, tsconfig.json, src/app/layout.tsx, src/app/globals.css</files>
  <read_first>.planning/phases/01-product-shell-and-writer-profile/01-CONTEXT.md, .planning/PROJECT.md, docs/plans/2026-03-30-source-aware-writing-agent-design.md</read_first>
  <action>Create a Next.js App Router project in this repository using TypeScript and a src-based app layout. Ensure package.json includes scripts named dev, build, lint, test, and typecheck. Keep existing .planning and docs directories untouched. Configure src/app/layout.tsx and src/app/globals.css with a simple application frame suited to a writing product, not a demo placeholder.</action>
  <verify>test -f package.json && test -f src/app/layout.tsx && test -f src/app/globals.css</verify>
  <acceptance_criteria>
    - package.json contains "next"
    - package.json contains "\"typecheck\""
    - src/app/layout.tsx exists and contains "Source-Aware Personal Writing Agent" or the chosen product title
    - src/app/globals.css exists and is not empty
  </acceptance_criteria>
  <done>Next.js app scaffold exists with the required scripts and a non-placeholder application frame.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the writer profile contract and persistence layer</name>
  <files>src/lib/writer-profile-schema.ts, src/lib/writer-profile-store.ts, src/app/api/profile/route.ts, tests/writer-profile-store.test.ts</files>
  <read_first>.planning/phases/01-product-shell-and-writer-profile/01-CONTEXT.md, .planning/REQUIREMENTS.md, package.json</read_first>
  <action>Define a WriterProfile type and schema with these exact fields: displayName, defaultOutput, defaultTone, bannedMarkers, writingNotes. Implement src/lib/writer-profile-store.ts as the single persistence boundary for this phase, using an app-owned local store rather than a hosted backend. Expose GET and POST handlers in src/app/api/profile/route.ts that validate against the shared schema. Add at least one automated test in tests/writer-profile-store.test.ts covering save and reload behavior.</action>
  <verify>npm test -- --runInBand writer-profile-store</verify>
  <acceptance_criteria>
    - src/lib/writer-profile-schema.ts contains "displayName"
    - src/lib/writer-profile-schema.ts contains "bannedMarkers"
    - src/app/api/profile/route.ts contains "export async function GET"
    - src/app/api/profile/route.ts contains "export async function POST"
    - tests/writer-profile-store.test.ts exists
  </acceptance_criteria>
  <done>The writer profile contract is shared, persisted, and covered by at least one automated store test.</done>
</task>

<task type="auto">
  <name>Task 3: Build the profile editing page on top of the shared store</name>
  <files>src/app/profile/page.tsx, src/app/api/profile/route.ts, src/lib/writer-profile-schema.ts, src/lib/writer-profile-store.ts</files>
  <read_first>src/app/api/profile/route.ts, src/lib/writer-profile-schema.ts, .planning/ROADMAP.md</read_first>
  <action>Create src/app/profile/page.tsx as a usable profile form, not a placeholder. The form must expose inputs for display name, default output target, default tone, banned markers, and writing notes. It must submit through the shared profile route and render previously saved values on reload. Keep copy aligned with the approved product language: personal writing agent, reconstruction over summarization, no emoji-heavy messaging.</action>
  <verify>npm run lint && npm run typecheck</verify>
  <acceptance_criteria>
    - src/app/profile/page.tsx contains "displayName"
    - src/app/profile/page.tsx contains "defaultTone"
    - src/app/profile/page.tsx contains "writingNotes"
    - src/app/profile/page.tsx does not contain "Coming soon"
  </acceptance_criteria>
  <done>The profile page is connected to real persistence and exposes the exact profile fields needed by later phases.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm test -- --runInBand writer-profile-store
- [ ] npm run lint
- [ ] npm run typecheck
- [ ] npm run build
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repository contains a real app shell and a persisted writer profile flow
</success_criteria>

<output>
After completion, create `.planning/phases/01-product-shell-and-writer-profile/01-01-SUMMARY.md`
</output>
