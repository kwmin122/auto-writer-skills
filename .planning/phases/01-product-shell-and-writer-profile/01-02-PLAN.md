---
phase: 01-product-shell-and-writer-profile
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/app/page.tsx
  - src/components/session-start-shell.tsx
  - src/components/source-input-examples.tsx
  - src/components/profile-summary-card.tsx
  - tests/session-start-shell.test.tsx
autonomous: true
requirements:
  - INPT-02
user_setup: []
must_haves:
  truths:
    - The first screen clearly explains what source types the product accepts before the user submits anything.
    - The saved writer profile is surfaced on the first screen so the app already feels personalized.
    - The home screen establishes the future writing-session flow without implementing Phase 2 parsing work.
  artifacts:
    - src/app/page.tsx renders the product landing and session-start shell.
    - src/components/source-input-examples.tsx contains concrete example content for all supported source types.
    - src/components/profile-summary-card.tsx displays the saved writer profile on the home screen.
    - tests/session-start-shell.test.tsx verifies example rendering or profile summary output.
  key_links:
    - The home page reads the persisted writer profile from Plan 01.
    - Example cards match the supported input categories listed in requirements and design docs.
---

<objective>
Build the first-screen experience that introduces supported inputs and shows the saved profile in context.

Purpose: The user should immediately understand how to start using the product and see that the app is oriented around their personal writing preferences.
Output: A home page, source example components, a profile summary card, and a basic UI test around the first-screen shell.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-product-shell-and-writer-profile/01-CONTEXT.md
@.planning/phases/01-product-shell-and-writer-profile/01-01-SUMMARY.md
@docs/plans/2026-03-30-source-aware-writing-agent-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the personalized session-start shell</name>
  <files>src/app/page.tsx, src/components/session-start-shell.tsx, src/components/profile-summary-card.tsx</files>
  <read_first>.planning/phases/01-product-shell-and-writer-profile/01-CONTEXT.md, src/app/profile/page.tsx, src/lib/writer-profile-schema.ts</read_first>
  <action>Create src/app/page.tsx as the product home screen and route it through src/components/session-start-shell.tsx. The shell should include a one-sentence explanation of reconstruction-focused writing, a profile summary card that displays displayName, defaultTone, and defaultOutput, and a visible session input area that previews future source collection without implementing parsing yet. Do not add fake generated content or "AI magic" marketing copy.</action>
  <verify>test -f src/app/page.tsx && test -f src/components/session-start-shell.tsx && test -f src/components/profile-summary-card.tsx</verify>
  <acceptance_criteria>
    - src/app/page.tsx contains "session-start-shell" or imports SessionStartShell
    - src/components/profile-summary-card.tsx contains "defaultTone"
    - src/components/profile-summary-card.tsx contains "defaultOutput"
    - src/components/session-start-shell.tsx does not contain "Coming soon"
  </acceptance_criteria>
  <done>The first screen feels like a real starting point for the product and reflects the saved writer profile.</done>
</task>

<task type="auto">
  <name>Task 2: Add concrete source-input examples for every supported type</name>
  <files>src/components/source-input-examples.tsx, src/components/session-start-shell.tsx, src/app/page.tsx</files>
  <read_first>.planning/REQUIREMENTS.md, docs/plans/2026-03-30-source-aware-writing-agent-design.md, src/components/session-start-shell.tsx</read_first>
  <action>Create src/components/source-input-examples.tsx and render it from the session-start shell. Include explicit example content for these exact categories: short note, URL, YouTube link, GitHub repository, paper link. Each example should show the kind of input the user can paste, not abstract labels only. Make the examples visible without any clicks.</action>
  <verify>rg -n \"short note|URL|YouTube|GitHub|paper\" src/components/source-input-examples.tsx</verify>
  <acceptance_criteria>
    - src/components/source-input-examples.tsx contains "YouTube"
    - src/components/source-input-examples.tsx contains "GitHub"
    - src/components/source-input-examples.tsx contains "paper"
    - src/components/source-input-examples.tsx contains at least one realistic example string per source type
  </acceptance_criteria>
  <done>The user can understand supported source types from the first screen alone.</done>
</task>

<task type="auto">
  <name>Task 3: Add a lightweight UI test for the first-screen contract</name>
  <files>tests/session-start-shell.test.tsx, src/app/page.tsx, src/components/session-start-shell.tsx, src/components/source-input-examples.tsx</files>
  <read_first>package.json, src/app/page.tsx, src/components/source-input-examples.tsx</read_first>
  <action>Add a UI-focused automated test in tests/session-start-shell.test.tsx that verifies the home screen renders the supported source examples and shows the profile summary labels. Use the project's chosen test runner from Plan 01 and keep assertions text-based so they remain stable during styling changes.</action>
  <verify>npm test -- --runInBand session-start-shell</verify>
  <acceptance_criteria>
    - tests/session-start-shell.test.tsx exists
    - tests/session-start-shell.test.tsx asserts presence of at least three supported source labels
    - tests/session-start-shell.test.tsx asserts presence of the profile summary area
  </acceptance_criteria>
  <done>The first-screen contract is protected by at least one lightweight automated test.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm test -- --runInBand session-start-shell
- [ ] npm run lint
- [ ] npm run typecheck
- [ ] npm run build
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The home screen visibly teaches supported inputs and reflects the saved profile
</success_criteria>

<output>
After completion, create `.planning/phases/01-product-shell-and-writer-profile/01-02-SUMMARY.md`
</output>
