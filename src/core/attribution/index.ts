export function buildAttributionRuleText() {
  return [
    "External facts and references must keep explicit source lines.",
    "Do not drop GitHub, YouTube, paper, or article links when they matter to the draft.",
    "If a source could not be parsed automatically, ask for one or two lines of manual context."
  ].join(" ");
}
